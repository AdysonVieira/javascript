// Operador Ternário | abreviação de condicionais com if e else
const idade = 19;
const possuiDiabetes = false

// condição ? true : false
const podeBeber = (idade >= 18 && possuiDiabetes === false) ? 'Pode beber' : 'Não pode beber'

console.log(podeBeber) // 'Pode beber'