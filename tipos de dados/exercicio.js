// Declare uma variável contendo uma string
const string = 'Contém uma string';

// Declare uma variável contendo um número dentro de uma string
const numeroString = '27';

// Declare uma variável com sua idade
const idade = 27;

// Declare duas variáveis, uma com seu nome, outra com seu sobrenome e some as mesmas
let nome = 'Adyson',
    sobrenome = 'Vieira',
    nomeCompleto = `${nome} ${sobrenome}`;
console.log(nomeCompleto)

// Coloque a seguinte frase em uma variável: It's time
const fraseAspas = "It's time";
const fraseEscape = 'It\'s moment';
const fraseTemplate = `It's start`;
console.log(fraseAspas, fraseEscape, fraseTemplate)

// Verifique o tipo da variável que contém seu nome
console.log(typeof nome);
